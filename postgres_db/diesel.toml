# For documentation on how to configure this file,
# see diesel.rs/guides/configuring-diesel-cli

[print_schema]
file = "src/schema.rs"
#import_types = ["diesel::sql_types::*", "crate::custom_types::sql_type_names::*"]
